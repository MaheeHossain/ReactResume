1. Create repo and clone it
2. Create two folders - client and server
3. In client - npx create-react-app ./
4. In server - npm init -y, npm install body-parser cors express mongoose nodemon
5. Server package.json - 
    "type": "module",
    "scripts": {
        "start": "nodemon index.js"
    },
6. In client - npm install axios moment react-file-base64 redux redux-thunk
7. In client - npm install --legacy-peer-deps